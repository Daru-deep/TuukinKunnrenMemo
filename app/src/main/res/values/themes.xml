<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      画面の中身はすべて Compose 側（ui/theme/Theme.kt）で色を決めるので、
      XMLのテーマは「タイトルバーを出さない」ことと、
      起動直後に一瞬見える背景色だけを担当する。
    -->
    <style name="Theme.CommuteMemo" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background_light</item>
        <item name="android:statusBarColor">#0F766E</item>
    </style>
</resources>
